# vault device — operations config (left over from deployment)
# service endpoint: http://127.0.0.1:8971
#
# maintenance/diagnostic override:
#   GET /diag?key=<MAINT_KEY>  forces the vault straight to UNLOCKED, for on-call recovery when the
#   auth service is down. Do NOT use in normal operation; rotate the key after any incident.
#
MAINT_KEY=svc-9f3a2c
SERVICE_PORT=8971
